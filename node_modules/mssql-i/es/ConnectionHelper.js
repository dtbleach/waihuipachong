"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var mssql_1 = require("mssql");
var ConnectionHelper = (function () {
    function ConnectionHelper() {
    }
    ConnectionHelper.create = function (connConfig) {
        var pool = new mssql_1.ConnectionPool(connConfig);
        return pool.connect();
    };
    ConnectionHelper.close = function (conn) {
        if (conn && (conn.connected || conn.connecting)) {
            return conn.close();
        }
    };
    return ConnectionHelper;
}());
exports.ConnectionHelper = ConnectionHelper;
