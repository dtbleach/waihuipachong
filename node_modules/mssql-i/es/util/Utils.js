"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Utils = (function () {
    function Utils() {
    }
    Utils.getDbObjectName = function (database, chema, objectName) {
        if (!database && !chema) {
            return "[" + objectName + "]";
        }
        else if (!chema) {
            return "[" + database + "]..[" + objectName + "]";
        }
        else if (!database) {
            return "[" + chema + "].[" + objectName + "]";
        }
        return "[" + database + "].[" + chema + "].[" + objectName + "]";
    };
    Utils.getDataBaseFromConnection = function (conn) {
        var config = Reflect.get(conn, "config");
        return config.database;
    };
    return Utils;
}());
exports.Utils = Utils;
