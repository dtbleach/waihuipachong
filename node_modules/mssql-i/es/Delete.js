"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = y[op[0] & 2 ? "return" : op[0] ? "throw" : "next"]) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [0, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
var mssql_1 = require("mssql");
var Schema_1 = require("./schema/Schema");
var Where_1 = require("./util/Where");
var Utils_1 = require("./util/Utils");
var Delete = (function () {
    function Delete() {
    }
    Delete.delete = function (conn, pars, tran) {
        return __awaiter(this, void 0, void 0, function () {
            var database, table, data, schemaModel, tableSchemaModel, whereList, wherePars, whereSQL, primaryKeyList, _i, primaryKeyList_1, column, key, tableName, sql, request;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        database = pars.database || Utils_1.Utils.getDataBaseFromConnection(conn);
                        table = pars.table;
                        if (!table) {
                            return [2, Promise.reject(new Error("pars.table can not be null or empty!"))];
                        }
                        data = pars.data;
                        if (!data) {
                            return [2, Promise.reject(new Error("pars.data can not be null or empty!"))];
                        }
                        return [4, Schema_1.Schema.getSchema(conn, database)];
                    case 1:
                        schemaModel = _a.sent();
                        tableSchemaModel = schemaModel.getTableSchemaModel(table);
                        if (!tableSchemaModel) {
                            return [2, Promise.reject(new Error("Table '" + table + "' is not exists!"))];
                        }
                        whereList = new Array();
                        wherePars = {};
                        whereSQL = "";
                        primaryKeyList = tableSchemaModel.columns.filter(function (column) { return column.primaryKey; });
                        if (primaryKeyList.length < 1) {
                            return [2, Promise.reject(new Error("Table '" + table + "' has no primary key, you can not call this function. Please try function 'deleteByWhere'!"))];
                        }
                        for (_i = 0, primaryKeyList_1 = primaryKeyList; _i < primaryKeyList_1.length; _i++) {
                            column = primaryKeyList_1[_i];
                            key = column.columnName;
                            if (Reflect.has(data, key)) {
                                whereSQL += " " + key + " = @wpar" + key + " and";
                                whereList.push(Reflect.get(data, key));
                                Reflect.set(wherePars, "wpar" + key, Reflect.get(data, key));
                            }
                            else {
                                return [2, Promise.reject(new Error("Key " + column.columnName + " is not provided!"))];
                            }
                        }
                        whereSQL = " where " + whereSQL.replace(/and$/, "");
                        tableName = Utils_1.Utils.getDbObjectName(database, pars.chema, table);
                        sql = "delete from " + tableName + " " + whereSQL;
                        if (tran) {
                            request = new mssql_1.Request(tran);
                        }
                        else {
                            request = conn.request();
                        }
                        Reflect.ownKeys(wherePars).map(function (m) {
                            request.input(m.toString(), Reflect.get(wherePars, m));
                        });
                        return [4, request.query(sql)];
                    case 2:
                        _a.sent();
                        return [2];
                }
            });
        });
    };
    Delete.deleteByWhere = function (conn, pars, tran) {
        return __awaiter(this, void 0, void 0, function () {
            var database, where, table, schemaModel, tableSchemaModel, _a, whereSQL, whereList, wherePars, tableName, sql, request;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        database = pars.database || Utils_1.Utils.getDataBaseFromConnection(conn);
                        where = pars.where;
                        table = pars.table;
                        if (!table) {
                            return [2, Promise.reject(new Error("pars.table can not be null or empty!"))];
                        }
                        return [4, Schema_1.Schema.getSchema(conn, database)];
                    case 1:
                        schemaModel = _b.sent();
                        tableSchemaModel = schemaModel.getTableSchemaModel(table);
                        if (!tableSchemaModel) {
                            return [2, Promise.reject(new Error("Table '" + table + "' is not exists!"))];
                        }
                        _a = Where_1.Where.getWhereSQL(where, tableSchemaModel), whereSQL = _a.whereSQL, whereList = _a.whereList, wherePars = _a.wherePars;
                        tableName = Utils_1.Utils.getDbObjectName(database, pars.chema, table);
                        sql = "delete from " + tableName + " " + whereSQL;
                        if (tran) {
                            request = new mssql_1.Request(tran);
                        }
                        else {
                            request = conn.request();
                        }
                        Reflect.ownKeys(wherePars).map(function (m) {
                            request.input(m.toString(), Reflect.get(wherePars, m));
                        });
                        return [4, request.query(sql)];
                    case 2:
                        _b.sent();
                        return [2];
                }
            });
        });
    };
    return Delete;
}());
exports.Delete = Delete;
